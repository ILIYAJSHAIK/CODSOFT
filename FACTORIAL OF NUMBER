def fact(number):
    m = 1
    i = 1
    while(i < number):
        i = i+1
        m = m*i
    print(f"Factorial of {number} is : {m}")
    
try :
    user_input = int(input("Enter the number : "))
    fact(user_input)
    
except ValueError :
    print("Please enter a valid number.")
