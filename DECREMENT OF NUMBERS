def decrement_num(number):
    num = number+1
    while(num > 0):
        num -= 1
        print(num)

try :
    user_input = int(input("Enter the number : "))
    decrement_num(user_input)

except ValueError :
    print("Please enter a valid number.")
